## Application properties (default)
## This file activates the 'local' profile by default for local development.
## Remove or override in production/environments where you don't want local config.

# Activate local profile so application-local.properties is loaded automatically
spring.profiles.active=local

## Other global properties can go here.
